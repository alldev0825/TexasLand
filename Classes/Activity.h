

#import <Foundation/Foundation.h>


@interface Activity : NSObject {

	NSString *receiptSubmitId,*chainId,*name,*offerId,*offerTitle,*pointsMultiplier,*submitDate,*receiptStatus,*submitted,*subTotal,*pointsEarned, *activityType, *amount, *restaurantrelevant;
}

@property (nonatomic,retain) NSString *receiptSubmitId,*chainId,*name,*offerId,*offerTitle,*pointsMultiplier,*submitDate,*receiptStatus,*submitted,*subTotal,*pointsEarned, *activityType, *amount, *restaurantrelevant ;
@end
